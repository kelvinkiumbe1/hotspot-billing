HELP.md
target/
.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/
build/
!**/src/main/**/build/
!**/src/test/**/build/

### VS Code ###
.vscode/

### App data (local database files, uploaded photos) ###
data/
uploads/

# Built UI copied in at image build time, never committed
src/main/resources/static/

# Per-tenant credentials and database dumps
deploy/tenants/
backups/

# Heavy stock-photo originals — use the resized copies in marketing/**/images/
*-unsplash.jpg

# Local-only secrets (M-Pesa sandbox keys), read via spring.config.import
local.properties

# Raw source videos (compress into marketing/**/images/ before committing)
/*_*fps.mp4

# Raw logo-maker export (processed assets live in each app)
Logo maker project*

# Built frontend baked into the backend jar (generated by deploy/build-local-app.sh)
/src/main/resources/static/
# LOCAL provisioner per-tenant runtime (logs, launched app instances)
/control-plane/local-tenants/
local-tenants/
